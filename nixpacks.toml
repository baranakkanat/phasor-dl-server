[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "ffmpeg", "deno"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python3.12 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
